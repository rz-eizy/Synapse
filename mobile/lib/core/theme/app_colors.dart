import 'package:flutter/material.dart';

class AppColors {
  // Colores Base (por ahora escala de grises hasta tener el logo)
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color appBarColor = Color(0xFF1F1F1F);

  // Colores de Acento (detalles en conjugación con mi logo synapse)
  static const Color primary = Color.fromARGB(255, 86, 187, 245); 
  static const Color secondary = Color(0xFF03DAC6); 
  
  // Texto
  static const Color textPrimary = Color(0xFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B3);

  // Estados
  static const Color error = Color(0xFFCF6679);


  static const Color bgLight = Color(0xFFFFFFFF);
  static const Color bgMedium = Color(0xFFE0E0E0);
  static const Color darkGray = Color(0xFF333333);
  static const Color mediumGray = Color(0xFF888888);
  static const Color lightGray = Color(0xFFF5F5F5);
}
